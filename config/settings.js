module.exports = {
  prefix: "&",

  // from_bot_permission: 70265920,
  // to_bot_permission: 70255616,

  // Why Add Administrator?
  // Because bot can't join private channels
  from_bot_permission: 8,
  to_bot_permission: 8,

  wait_reaction_sec: 15
}
